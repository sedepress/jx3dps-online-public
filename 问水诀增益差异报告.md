# 问水诀启用增益差异报告

记录时间：2026-07-02

## 结论

问水诀启用增益后的底层计算公式，和其他心法共用同一套逻辑。当前差异主要来自三类配置：

- 问水诀默认快捷增益按 Excel 当前口径写了更精确的层数和覆盖率。
- 问水诀循环详情会覆盖部分装备特效和团队增益覆盖率。
- 问水诀技能系数里有心法专属的奇穴/手法/秘籍增益。

因此这里的差异优先按“配置口径差异”处理，不应先假设通用增益算法有问题。

## 共用计算链路

启用增益后，通用计算入口在 `src/计算模块/循环秒伤计算.ts`：

- 先用 `获取装备增益(装备信息, 快照计算, 计算循环详情?.覆盖率)` 汇总装备类增益。
- 若 `增益启用` 为 true，再调用 `根据增益选项获取增益集合(增益数据, undefined, 快照计算, 计算循环详情?.增益覆盖率)` 汇总小吃、团队增益、阵眼。
- 团队增益的实际值统一按 `基础值 * 层数 * 覆盖率 / 100` 计算。
- `飘黄` 会额外往循环中追加真实伤害技能；问水诀使用 `逐云含蕊`，其他没有该技能系数的心法会走 `逐云寒蕊`。

相关文件：

- `src/计算模块/循环秒伤计算.ts`
- `src/计算模块/统一工具函数/增益计算函数.ts`
- `src/数据/团队增益/通用/index.ts`
- `src/数据/团队增益/外功/index.ts`

## 问水诀开荒拉满与其他外功心法的主要差异

问水诀配置文件：

- `src/心法模块/心法/问水诀/默认数据/增益快捷设置数据/开荒拉满.json`

主要差异如下：

| 增益项   | 问水诀开荒拉满                                    | 其他外功心法常见口径                                                | 备注                                                                      |
| -------- | ------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 阵眼     | `凌雪阵`                                          | 凌海用 `蓬莱阵(阵眼)`，孤锋用 `刀宗阵(阵眼)`，部分外功也用 `凌雪阵` | 阵眼按心法默认配置不同                                                    |
| 小吃     | 身法、外攻、破防组合，含 `创意料理（-气血+破防）` | 很多外功使用会心/无双或外攻小吃组合                                 | 属于默认收益选择差异                                                      |
| 朝圣     | 启用，`100层 / 2.66666666666667%`                 | 多数外功开荒拉满里关闭，默认覆盖率常见 `7%`                         | 问水按 Excel 当前覆盖率                                                   |
| 圣浴明心 | 关闭，`100层 / 4.5%`                              | 多数外功开荒拉满里启用                                              | 与朝圣冲突口径不同                                                        |
| 寒啸千军 | 启用，`1层 / 50%`                                 | 很多旧外功配置为 `100层 / 47%` 或 `100层 / 50%`                     | 问水已按“无双百分比 51 \* 覆盖率”处理，后续要重点确认是否需要统一其他心法 |
| 仙王蛊鼎 | `100层 / 24.6913580246914%`                       | 常见 `100层 / 25%`                                                  | 问水按 Excel 当前覆盖率                                                   |
| 号令三军 | `100层 / 21.4285714285714%`                       | 常见 `100层 / 20%`                                                  | 问水循环详情还可能再覆盖为 `20.4545454545455%`                            |
| 飘黄     | `100层 / 58.252427184466%`                        | 常见 `100层 / 50%`                                                  | 面板增益和追加技能数量都要看循环时间口径                                  |
| 破甲     | `1层 / 33.3333333333333%`                         | 常见 `1层 / 33%`                                                    | 精度差异                                                                  |
| 皎素     | 关闭                                              | 多数外功开荒拉满里启用                                              | 默认选项差异                                                              |
| 配伍     | 关闭                                              | 多数外功开荒拉满里启用                                              | 默认选项差异                                                              |

## 循环详情覆盖率差异

问水诀部分循环会在循环详情内覆盖通用配置。

例如 `src/心法模块/心法/问水诀/计算循环/叠锋意碧归循环.json`：

- `覆盖率.风特效腰坠 = 0.136363636363636`
- `覆盖率.大附魔_伤腰 = 0.9367786241920606`
- `增益覆盖率.号令三军 = 20.4545454545455`
- `技能数量计算时间 = 220`
- `战斗时间 = 219.51`

这意味着即使快捷增益里号令三军覆盖率是 `21.4285714285714`，进入该循环计算时也会优先使用循环详情里的 `20.4545454545455`。

## 问水诀心法专属增益

问水诀技能增益文件：

- `src/心法模块/心法/问水诀/技能增益/通用增益.ts`
- `src/心法模块/心法/问水诀/技能增益/秘籍增益.ts`

关键心法专属项：

- `非侠`: `154 / 1024`
- `岱宗`: 会效增益，支持循环覆盖率字段 `岱宗`
- `雾锁`: 部分技能数启用，增加无视防御和外功基础攻击
- `造化1 / 造化2`: 部分技能数启用的通用增伤
- `叠锋意·1 / 2 / 3`: 部分技能数启用的会心和突破上限会效
- `碧归`: 部分技能数启用的 `307 / 1024` 通用增伤
- `手法伤害`: 职业全局增益 `8 / 1024`
- `怜光`: 重剑技能全局增益 `102 / 1024`

这些不属于团队增益面板差异，但会让问水诀最终 DPS 与其他心法不同。

## 快照建模差异

很多旧心法的通用增益会引入 `src/心法模块/统一数据/快照增益.ts`，其中包含：

- `朝圣_快照`
- `圣浴明心_快照`
- `号令三军_快照_一鼓`
- `号令三军_快照_二鼓`
- `仙王蛊鼎_快照`
- `寒啸千军_快照`
- `飘黄_快照`
- `风特效_快照`
- `大附魔_伤腰_快照`

问水诀当前没有直接在 `技能增益/通用增益.ts` 引入这套统一快照模板，而是更多依赖循环 JSON 中显式拆分的 `技能增益列表` 和循环覆盖率。这是后续如果要统一增益模型时需要重点检查的地方。

## 后续修改建议

1. 先决定目标口径：问水诀继续对齐 Excel 当前藏剑计算器，还是向其他外功心法默认快捷配置靠齐。
2. 单独确认 `寒啸千军` 的层数语义。问水诀当前是 `1层 / 50%`，避免把无双百分比错误放大成 `100层`。
3. 若改快捷增益默认值，需要同时检查循环详情里的 `增益覆盖率` 是否还应该覆盖。
4. 若考虑引入统一快照增益，需要先写问水诀回归测试，避免破坏现有 Excel 对齐用例。
5. 修改前优先跑：

```bash
./node_modules/.bin/jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand
```
