import { 装备部位枚举 } from '@/@types/枚举'
import { 属性类型 } from '@/@types/属性'
import { 附魔原始数据, 附魔数据类型 } from '@/@types/附魔'

const 原始数据: 附魔原始数据[] = [
  { 数值: 978, 分数: 1341, 颜色: '蓝' },
  { 数值: 836, 分数: 1146, 颜色: '蓝', 赛季: '上赛季' },
]

export const 获取衣服腰带节日附魔数据 = (): 附魔数据类型[] => {
  return 原始数据.map((item) => {
    return {
      附魔名称: `无双+${item?.数值}`,
      附魔支持部位: [装备部位枚举.衣服, 装备部位枚举.腰带],
      增益集合: [{ 属性: 属性类型.无双等级, 值: item?.数值 }],
      附魔装分: item?.分数,
      附魔颜色: item?.颜色,
    }
  })
}
