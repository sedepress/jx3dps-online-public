import { 属性类型 } from '@/@types/属性'
import { 技能增益列表类型 } from '@/@types/技能'

const 伤害秘籍值: Record<string, number> = {
  '5%伤害': 51,
  '4%伤害': 41,
  '3%伤害': 31,
  '2%伤害': 20,
  '20%伤害': 205,
  三柴伤害204: 204,
}

const 会心秘籍值: Record<string, number> = {
  '4%会心': 0.04,
  '3%会心': 0.03,
  '2%会心': 0.02,
}

const 创建伤害秘籍 = (增益名称: string, 值: number): 技能增益列表类型 => ({
  增益名称,
  增益所在位置: '秘籍',
  增益类型: '全局启用',
  增益集合: [{ 属性: 属性类型.通用增伤, 值: 值 / 1024 }],
})

const 创建会心秘籍 = (增益名称: string, 值: number): 技能增益列表类型 => ({
  增益名称,
  增益所在位置: '秘籍',
  增益类型: '全局启用',
  增益集合: [{ 属性: 属性类型.外功会心百分比, 值 }],
})

const 问水秘籍增益映射: Record<string, 技能增益列表类型> = {
  ...Object.fromEntries(
    Object.entries(伤害秘籍值).map(([名称, 值]) => [名称, 创建伤害秘籍(名称, 值)]),
  ),
  ...Object.fromEntries(
    Object.entries(会心秘籍值).map(([名称, 值]) => [名称, 创建会心秘籍(名称, 值)]),
  ),
}

export const 获取问水秘籍增益 = (增益名称列表: string[]): 技能增益列表类型[] => {
  return 增益名称列表.map((增益名称) => 问水秘籍增益映射[增益名称]).filter(Boolean)
}

export const 大橙武技能增伤: 技能增益列表类型 = {
  增益名称: '大橙武技能增伤',
  增益所在位置: '装备',
  增益启用: false,
  增益类型: '全局启用',
  增益集合: [{ 属性: 属性类型.通用增伤, 值: 51 / 1024 }],
}

export const 怜光增益: 技能增益列表类型 = {
  增益名称: '怜光',
  增益所在位置: '奇穴',
  增益类型: '全局启用',
  增益集合: [{ 属性: 属性类型.通用增伤, 值: 102 / 1024 }],
}

export const 层云增益: 技能增益列表类型 = {
  增益名称: '层云',
  增益所在位置: '奇穴',
  增益类型: '全局启用',
  增益集合: [{ 属性: 属性类型.通用增伤, 值: 415.9375 / 1024 }],
}
