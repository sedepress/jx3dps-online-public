import { 增益选项数据类型 } from '@/@types/团队增益'
import { 属性加成, 属性类型 } from '@/@types/属性'
import { 循环技能详情, 技能增益列表数据 } from '@/@types/循环'
import { 快照类型, 快照类型数据, 技能基础数据模型, 技能计算增益数据列表 } from '@/@types/技能'
import { 常驻面板类装备特效, 装备信息数据类型, 装备增益类型, 装备增益数据类型 } from '@/@types/装备'
import 获取当前数据 from '@/数据/数据工具/获取当前数据'
import { 优化版通用增益计算 } from './通用增益计算'
import 获取装备特效增益数据 from '@/数据/数据工具/获取装备特效增益数据'
import { 最终计算属性类型 } from '@/@types/计算'
import { 自身属性系数 } from '@/数据/常量'
import { 获取属性加成后的面板 } from '../非郭氏计算/非郭氏技能总伤害计算'

const 面板特效覆盖率配置 = ['试炼鞋子会心', '试炼鞋子破防', '特效_40803']

const 获取当前装备增益数据 = () => (获取当前数据()?.装备增益数据 || {}) as Partial<装备增益数据类型>

// 统计增益，获取增益的集合
export const 获取装备增益 = (
  装备信息: 装备信息数据类型,
  快照计算: 快照类型[],
  循环覆盖率?: { [key: string]: number },
): 属性加成[] => {
  const 装备增益数据 = 获取当前装备增益数据() as 装备增益数据类型
  const { 覆盖率 = {} } = 获取当前数据()
  let 总增益集合: 属性加成[] = []
  // 如果是快照计算，不计算套装、水特效、风腰椎的加成。放到最后的增益数量里计算
  if (!快照计算?.includes('套装会心会效')) {
    if (装备信息?.装备增益?.套装会心会效) {
      // 判断套装覆盖率
      const 套装覆盖率 = 循环覆盖率?.套装会心会效 || 覆盖率?.套装会心会效 || 0.6
      const 装备特效增益效果 = 获取装备特效增益数据('套装会心会效', 1, 装备增益数据, 套装覆盖率)
      if (装备特效增益效果) {
        总增益集合 = 总增益集合.concat(装备特效增益效果)
      }
    }
  }
  if (!快照计算?.includes('风特效')) {
    // 兼容旧代码的缓存
    if (装备信息?.装备增益?.风特效腰坠) {
      const 增益等级 =
        循环覆盖率?.风特效腰坠等级 ||
        装备信息?.装备增益?.风特效腰坠_实际等级 ||
        装备信息?.装备增益?.风特效腰坠
      // 判断套装覆盖率
      const 套装覆盖率 = 循环覆盖率?.风特效腰坠 ?? 覆盖率?.风特效腰坠 ?? 15 / 180
      const 装备特效增益效果 = 获取装备特效增益数据(
        '风特效腰坠',
        增益等级,
        装备增益数据,
        套装覆盖率,
      )
      if (装备特效增益效果) {
        总增益集合 = 总增益集合.concat(装备特效增益效果)
      }
    }
  }
  if (!快照计算?.includes('大附魔_伤腰')) {
    if (装备信息?.装备增益?.大附魔_伤腰) {
      const 腰带覆盖率 = 循环覆盖率?.大附魔_伤腰 ?? 1
      const 腰带档位 = 装备信息.装备增益.大附魔_伤腰
      const 装备特效增益效果 = 获取装备特效增益数据(
        '大附魔_伤腰',
        腰带档位,
        装备增益数据,
        腰带覆盖率,
      )
      if (装备特效增益效果) {
        总增益集合 = 总增益集合.concat(装备特效增益效果)
      }
    }
  }
  if (装备信息?.装备增益?.切糕会心) {
    const 装备特效增益效果 = 获取装备特效增益数据('切糕会心', 1, 装备增益数据)
    if (装备特效增益效果) {
      总增益集合 = 总增益集合.concat(装备特效增益效果)
    }
  }
  if (装备信息?.装备增益?.切糕无双) {
    const 装备特效增益效果 = 获取装备特效增益数据('切糕无双', 1, 装备增益数据)
    if (装备特效增益效果) {
      总增益集合 = 总增益集合.concat(装备特效增益效果)
    }
  }
  if (装备信息?.装备增益?.切糕会心_英雄) {
    const 装备特效增益效果 = 获取装备特效增益数据('切糕会心', 2, 装备增益数据)
    if (装备特效增益效果) {
      总增益集合 = 总增益集合.concat(装备特效增益效果)
    }
  }
  if (装备信息?.装备增益?.切糕无双_英雄) {
    const 装备特效增益效果 = 获取装备特效增益数据('切糕无双', 1, 装备增益数据)
    if (装备特效增益效果) {
      总增益集合 = 总增益集合.concat(装备特效增益效果)
    }
  }
  /**
   * 装备特效计算
   * 常驻增益、大橙武无双、试炼之地常驻、精简常驻
   */
  总增益集合 = 获取装备特效增益(总增益集合, 装备信息?.装备增益, 循环覆盖率)

  if (装备信息?.装备增益?.郭氏力道) {
    总增益集合 = 总增益集合.concat([
      { 属性: 属性类型.郭氏力道, 值: 装备信息?.装备增益?.郭氏力道 || 0 },
    ])
  }
  if (装备信息?.装备增益?.郭氏身法) {
    总增益集合 = 总增益集合.concat([
      { 属性: 属性类型.郭氏身法, 值: 装备信息?.装备增益?.郭氏身法 || 0 },
    ])
  }
  if (装备信息?.装备增益?.郭氏根骨) {
    总增益集合 = 总增益集合.concat([
      { 属性: 属性类型.郭氏根骨, 值: 装备信息?.装备增益?.郭氏根骨 || 0 },
    ])
  }
  if (装备信息?.装备增益?.郭氏元气) {
    总增益集合 = 总增益集合.concat([
      { 属性: 属性类型.郭氏元气, 值: 装备信息?.装备增益?.郭氏元气 || 0 },
    ])
  }

  return 总增益集合
}

/**
 * @name 获取装备特效增益
 */
const 获取装备特效增益 = (
  总增益集合: 属性加成[],
  装备增益: 装备增益类型,
  循环覆盖率?: { [key: string]: number },
) => {
  const 装备增益数据 = 获取当前装备增益数据() as 装备增益数据类型
  let 最终增益集合 = [...总增益集合]
  常驻面板类装备特效?.forEach((增益类型) => {
    if (装备增益?.[增益类型]) {
      const 增益等级 = 获取装备特效增益等级(装备增益, 增益类型)
      const 装备特效增益效果 = 获取装备特效增益数据(
        增益类型,
        增益等级,
        装备增益数据,
        获取常驻面板类装备特效覆盖率(增益类型, 循环覆盖率),
      )
      if (装备特效增益效果) {
        最终增益集合 = 最终增益集合.concat(装备特效增益效果)
      }
    }
  })
  return 最终增益集合
}

const 获取装备特效增益等级 = (装备增益: 装备增益类型, 增益类型: string) => {
  return 装备增益?.[`${增益类型}_等级列表`] || 装备增益?.[增益类型]
}

const 获取常驻面板类装备特效覆盖率 = (增益类型: string, 循环覆盖率?: { [key: string]: number }) => {
  if (!面板特效覆盖率配置.includes(增益类型)) {
    return 1
  }
  return 循环覆盖率?.[增益类型] ?? 1
}

const 替换循环技能名称 = (循环: 循环技能详情[], 识别名称: string, 实际名称: string) => {
  return 循环.map((item) =>
    item.技能名称?.includes(识别名称) ? { ...item, 技能名称: 实际名称 } : item,
  )
}

const 替换巽护腕技能名称 = (循环: 循环技能详情[], 实际名称: string) => {
  return 循环.map((item) =>
    item.技能名称?.includes('巽') || item.技能名称 === '特效·腕'
      ? { ...item, 技能名称: 实际名称 }
      : item,
  )
}

// 根据增益信息修改最终循环内容
export const 根据增益修改最终循环 = (
  装备信息: 装备信息数据类型,
  计算循环,
  战斗时间,
  计算团辅和装备后属性: 最终计算属性类型,
): 循环技能详情[] => {
  let 最终循环: 循环技能详情[] = [...计算循环]
  if (装备信息?.装备增益?.大附魔_伤腕) {
    const 实际伤害名称 = 装备信息?.装备增益?.大附魔_伤腕 === 2 ? '昆吾·弦刃·英雄' : '昆吾·弦刃'
    最终循环 = 替换循环技能名称(最终循环, '昆吾·弦刃', 实际伤害名称)
    if (!最终循环?.some((item) => item.技能名称?.includes('昆吾·弦刃'))) {
      最终循环.push({
        技能名称: 实际伤害名称,
        技能数量: Math.floor(战斗时间 / 15),
      })
    }
  } else {
    最终循环 = 最终循环.filter((item) => !item.技能名称?.includes('昆吾·弦刃'))
  }

  if (装备信息?.装备增益?.大附魔_伤鞋) {
    const 实际伤害名称 = 装备信息?.装备增益?.大附魔_伤鞋 === 2 ? '刃凌·英雄' : '刃凌'
    最终循环 = 替换循环技能名称(最终循环, '刃凌', 实际伤害名称)
    if (!最终循环?.some((item) => item.技能名称?.includes('刃凌'))) {
      最终循环.push({
        技能名称: 实际伤害名称,
        技能数量: Math.floor(战斗时间 / 10),
      })
    }
  } else {
    最终循环 = 最终循环.filter((item) => !item.技能名称?.includes('刃凌'))
  }

  if (装备信息?.装备增益?.试炼暗器特效) {
    if (!最终循环?.some((item) => item.技能名称?.includes('无修·玄'))) {
      const 实际伤害名称 = `无修·玄·${装备信息?.装备增益?.试炼暗器特效 || 1}`
      最终循环.push({
        技能名称: 实际伤害名称,
        技能数量: Math.floor(战斗时间 / 10),
      })
    }
  } else {
    最终循环 = 最终循环.filter((item) => !item.技能名称?.includes('无修·玄'))
  }

  最终循环 = 最终循环.map((item) => {
    if (item.技能名称 !== '无修·荒' || !装备信息?.装备增益?.试炼暗器特效) {
      return item
    }
    return {
      ...item,
      技能等级: 装备信息.装备增益.试炼暗器特效,
    }
  })

  if (装备信息?.装备增益?.特效_42767) {
    if (!最终循环?.some((item) => item.技能名称?.includes('速·震'))) {
      // 12跳(9232)15跳(19285)17跳(30158)22跳(42057)
      const 实际跳数 = 速震特效跳数(计算团辅和装备后属性)
      const 实际伤害名称 = 装备信息?.装备增益?.特效_42767 > 2 ? `速·震·2` : `速·震`
      最终循环.push({
        技能名称: 实际伤害名称,
        技能数量: Math.floor((战斗时间 / 40) * 实际跳数),
      })
    }
  } else {
    最终循环 = 最终循环.filter((item) => !item.技能名称?.includes('速·震'))
  }

  if (装备信息?.装备增益?.特效_40788) {
    const 实际伤害名称 = `巽·${装备信息?.装备增益?.特效_40788 || 1}`
    const 静态特效腕 = 最终循环?.find((item) => item.技能名称 === '特效·腕')
    最终循环 = 替换巽护腕技能名称(最终循环, 实际伤害名称)
    if (!最终循环?.some((item) => item.技能名称?.includes('巽'))) {
      最终循环.push({
        技能名称: 实际伤害名称,
        技能数量: 静态特效腕?.技能数量 || Math.floor(战斗时间 / 10),
      })
    }
  } else {
    最终循环 = 最终循环.filter(
      (item) => !item.技能名称?.includes('巽') && item.技能名称 !== '特效·腕',
    )
  }

  if (装备信息?.装备增益?.龙门飞剑武器) {
    最终循环.push({
      技能名称: '剑风',
      技能数量: Math.floor((战斗时间 * 6) / 30),
    })
  }

  return 最终循环
}

export const 根据动态数量修改循环 = (
  计算循环: 循环技能详情[],
  计算团辅和装备后属性: 最终计算属性类型,
  装备增益?: 装备增益类型,
  伤害计算口径?: 'Excel小数破防防御',
): 循环技能详情[] => {
  const { 计算人物面板, 最终计算属性 } = 获取属性加成后的面板(
    计算团辅和装备后属性,
    undefined,
    装备增益,
  )

  return 计算循环.map((循环) => {
    if (!循环?.动态技能数量) {
      return 循环
    }

    const 基础会心率 =
      伤害计算口径 === 'Excel小数破防防御'
        ? Math.round(
            ((计算人物面板?.会心等级 || 0) / 自身属性系数.会心 + (最终计算属性?.额外会心率 || 0)) *
              10000,
          ) / 10000
        : (计算人物面板?.会心等级 || 0) / 自身属性系数.会心 + (最终计算属性?.额外会心率 || 0)

    const 会心率 = Math.min(1, 基础会心率 + (循环.动态技能数量.固定会心率 || 0))
    const 技能数量 =
      循环.动态技能数量.计算类型 === '会心间隔触发'
        ? 循环.动态技能数量.基础数量 /
          ((循环.动态技能数量.基础间隔 || 0) + 1 / ((循环.动态技能数量.触发系数 || 1) * 会心率))
        : 循环.动态技能数量.基础数量 * 会心率

    return {
      ...循环,
      技能数量,
      技能增益列表: 循环.技能增益列表?.map((增益) => ({
        ...增益,
        增益技能数: 技能数量,
      })),
    }
  })
}

// 对增益进行排序
export const 增益排序 = (list: 属性加成[]): 属性加成[] => {
  const SortKeyList = Object.values(属性类型)

  const newList = [...list]

  newList.sort((a, b) => {
    return SortKeyList.indexOf(a.属性) - SortKeyList.indexOf(b.属性)
  })

  return newList.filter((item) => !!item)
}

// 对增益进行同类合并，然后再排序返回
export const 增益合并并排序 = (list: 属性加成[]): 属性加成[] => {
  const obj = {}
  const skillCoefficientList: 属性加成[] = []
  list.forEach((item) => {
    if (item) {
      if (obj[item.属性]) {
        // 系数增伤不合并排序，否则多次计算会遗漏 目前只有系数增伤是乘算，其他的都为加算，若存在其他加算，这里也要过滤
        if (item.属性 !== 属性类型.系数增伤) {
          obj[item.属性] += item.值
        } else {
          skillCoefficientList.push(item)
        }
      } else {
        obj[item.属性] = item.值
      }
    }
  })
  const SortKeyList = Object.values(属性类型)

  const newList: any[] = Object.keys(obj).map((key) => {
    return { 属性: key, 值: obj[key] }
  })

  newList.sort((a, b) => {
    return SortKeyList.indexOf(a.属性) - SortKeyList.indexOf(b.属性)
  })

  return newList.filter((item) => !!item).concat(skillCoefficientList || [])
}

/**
 * 计算增益选项带来的增益。获取增益集合
 */
export const 根据增益选项获取增益集合 = (
  增益数据: 增益选项数据类型,
  过滤阵眼触发类增益?: boolean,
  快照计算?: string[],
  增益覆盖率?: { [key: string]: number },
): 属性加成[] => {
  const { 小药小吃 = [], 团队增益 = [], 阵眼 = [] } = 获取当前数据()
  let 增益集合: 属性加成[] = []

  if (增益数据?.小吃) {
    const 小吃数据集合 = 小药小吃.filter((item) => 增益数据?.小吃?.includes(item.小吃名称))
    if (小吃数据集合?.length) {
      小吃数据集合.forEach((a) => {
        if (a?.增益集合?.length) {
          增益集合 = 增益集合.concat(a?.增益集合)
        }
      })
    }
  }

  if (增益数据?.团队增益?.length) {
    const 团队增益集合 = 增益数据?.团队增益
      ?.filter((item) => item.启用 && !快照计算?.includes(item.增益名称))
      .map((item) => {
        const data: any = 团队增益.find((a) => a.增益名称 === item.增益名称)
        const 层数 = data?.层数选项数组?.length ? item?.层数 : data?.层数
        return {
          ...data,
          增益集合: data?.增益集合.map((c) => {
            const 覆盖率 = 增益覆盖率?.[item.增益名称] ?? item?.覆盖率
            return {
              ...c,
              值: (c?.值 * 层数 * 覆盖率) / 100,
            }
          }),
        }
      })

    if (团队增益集合?.length) {
      团队增益集合.forEach((item) => {
        增益集合 = 增益集合.concat(item.增益集合)
      })
    }
  }

  if (增益数据?.阵眼) {
    const 增益阵眼 = 阵眼.find((item) => item.阵眼名称 === 增益数据?.阵眼)
    if (增益阵眼 && 增益阵眼?.增益集合?.length) {
      if (过滤阵眼触发类增益) {
        增益集合 = 增益集合.concat(增益阵眼.增益集合.filter((增益) => !增益?.触发型增益))
      } else {
        增益集合 = 增益集合.concat(增益阵眼.增益集合)
      }
    }
  }

  return 增益集合
}

/**
 * @name 该技能数量下同时计算的多个增益的增益集合
 * getGainList
 */
export const 计算该技能下多个增益的增益集合 = (
  增益: 技能增益列表数据,
  当前技能属性: 技能基础数据模型,
  装备增益: 装备增益类型,
  快照计算: 快照类型[],
  团队增益结合: string[],
) => {
  // 将该数量下同时计算的多个增益转为数组
  const 该技能增益列表 = 增益.增益名称.split(',')

  // 该技能数量下同时计算的多个增益的增益集合
  const 增益名称列表: string[] = []

  该技能增益列表.forEach((i) => {
    const 该技能增益 = (当前技能属性?.技能增益列表 || [])?.find((item) => {
      if (item.增益名称 === i && item.增益类型 === '部分启用') {
        if (item?.增益所在位置 === '奇穴') {
          return item?.增益启用
        } else if (item?.依赖装备增益) {
          if (item?.快照类型) {
            // 先写的恶心点 为了兼容缓存内的处理
            if (item?.依赖装备增益?.includes('风特效腰坠')) {
              if (item?.依赖装备增益 === '风特效腰坠_英雄') {
                const 判断增益: any = { ...装备增益 }
                if (判断增益?.风特效腰坠_英雄 || 判断增益?.风特效腰坠 === 2) {
                  判断增益.风特效腰坠 = 2
                }
                return 判断增益?.['风特效腰坠'] === 2 && 快照计算?.includes(item?.快照类型)
              } else {
                return 装备增益?.['风特效腰坠'] === 1 && 快照计算?.includes(item?.快照类型)
              }
            } else if (item?.依赖装备增益?.includes('水特效腰坠')) {
              if (item?.依赖装备增益 === '水特效腰坠_英雄') {
                const 判断增益: any = { ...装备增益 }
                if (判断增益?.水特效腰坠_英雄 || 判断增益?.水特效腰坠 === 2) {
                  判断增益.水特效腰坠 = 2
                }
                return 判断增益?.['水特效腰坠'] === 2 && 快照计算?.includes(item?.快照类型)
              } else {
                return 装备增益?.['水特效腰坠'] === 1 && 快照计算?.includes(item?.快照类型)
              }
            } else {
              return !!装备增益?.[item?.依赖装备增益] && 快照计算?.includes(item?.快照类型)
            }
          } else {
            return !!装备增益?.[item?.依赖装备增益]
          }
        } else if (item?.依赖团队增益) {
          if (item?.快照类型) {
            return 快照计算?.includes(item?.快照类型) && item?.增益启用
          } else {
            return 团队增益结合?.includes(item?.依赖团队增益) && item?.增益启用
          }
        } else if (item?.依赖阵眼增益) {
          return 团队增益结合?.includes(item?.依赖阵眼增益)
        } else {
          return true
        }
      }
      return false
    })

    if (该技能增益) {
      增益名称列表.push(i)
    }
  })

  return 增益名称列表
}

export const 获取增益实际数据集合 = (增益名称列表: string[], 当前技能属性: 技能基础数据模型) => {
  let 增益集合列表: 技能计算增益数据列表[] = []
  增益名称列表.forEach((增益名称) => {
    const 该技能增益 = 当前技能属性?.技能增益列表?.find((item) => item?.增益名称 === 增益名称)
    const 该技能增益列表: 技能计算增益数据列表[] = (该技能增益?.增益集合 || []).map((item) => {
      return {
        ...item,
        增益启用: true,
        增益来源: 该技能增益?.增益名称,
      }
    })
    增益集合列表 = 增益集合列表.concat(该技能增益列表)
  })
  return 增益集合列表
}

export const 根据循环判断快照计算列表 = (循环技能数据: 循环技能详情[] | undefined): any[] => {
  const { 技能增益 } = 获取当前数据()
  if (!循环技能数据?.length) {
    return []
  }
  const 快照计算列表: string[] = []
  const 字符数据 = JSON.stringify(循环技能数据)
  快照类型数据.forEach((item) => {
    if (字符数据?.includes(item) && item !== '套装会心会效') {
      快照计算列表.push(item)
    }
  })

  // 判断套装双会
  if (技能增益?.通用增益?.length) {
    const 套装增益名称 = 技能增益?.通用增益?.find(
      (a) => a?.增益所在位置 === '装备' && a?.快照类型 === '套装会心会效',
    )?.增益名称

    if (套装增益名称) {
      const 存在套装Buff = 循环技能数据?.some((a) =>
        a?.技能增益列表?.some((b) => b?.增益名称?.includes(套装增益名称)),
      )
      if (存在套装Buff) {
        快照计算列表.push('套装会心会效')
      }
    }
  }

  return 快照计算列表
}

const 速震特效跳数 = (计算团辅和装备后属性: 最终计算属性类型): number => {
  // 12跳(9232)15跳(19285)17跳(30158)22跳(42057)
  const 加速 = 计算团辅和装备后属性.最终人物属性.加速等级 || 0
  let 跳数 = 12
  if (加速 <= 9232) {
    跳数 = Math.floor(12 + 3.249566724436742e-4 * 加速)
  } else if (加速 <= 19285) {
    跳数 = Math.floor(15 + 1.9894558838157763e-4 * (加速 - 9232))
  } else if (加速 <= 30158) {
    跳数 = Math.floor(17 + 2.759128115515497e-4 * (加速 - 19285))
  } else if (加速 <= 42057) {
    跳数 = Math.floor(20 + 1.6808135137406504e-4 * (加速 - 30158))
  } else {
    跳数 = 22
  }
  return 跳数
}

export const 通用增益计算 = 优化版通用增益计算

/**
 * 计算不同的增益对属性、技能增伤的影响
 * 返回最终参与技能伤害计算的人物属性、技能增伤等数据
 * 计算A类增伤，所有增伤害相加
 */
// export const 通用增益计算 = (增益: 属性加成, 结果属性: 最终计算属性类型): 最终计算属性类型 => {
//   const { 值, 属性 } = 增益
//   // const 结果属性 = { ...最终计算属性 }
//   let 破招转非侠 = 0

//   switch (属性) {
//     // 基础属性
//     case 属性类型.全属性:
//       结果属性.最终人物属性.力道 += 值
//       结果属性.最终人物属性.根骨 += 值
//       结果属性.最终人物属性.身法 += 值
//       结果属性.最终人物属性.元气 += 值
//       结果属性.最终人物属性.体质 += 值
//       break
//     case 属性类型.力道:
//       结果属性.最终人物属性.力道 += 值
//       break
//     case 属性类型.体质:
//       结果属性.最终人物属性.体质 += 值
//       break
//     case 属性类型.郭氏体质:
//       结果属性.气血加成.郭氏体质 += 值
//       break
//     case 属性类型.郭氏基础气血上限:
//       结果属性.气血加成.郭氏基础气血上限 += 值
//       break
//     case 属性类型.郭氏最终气血上限:
//       结果属性.气血加成.郭氏最终气血上限 += 值
//       break
//     case 属性类型.基础气血上限:
//       结果属性.最终人物属性.基础气血上限 += 值
//       break
//     case 属性类型.额外气血上限:
//       结果属性.最终人物属性.额外气血上限 += 值
//       break
//     case 属性类型.根骨:
//       结果属性.最终人物属性.根骨 += 值
//       break
//     case 属性类型.身法:
//       结果属性.最终人物属性.身法 += 值
//       break
//     case 属性类型.元气:
//       结果属性.最终人物属性.元气 += 值
//       break
//     case 属性类型.郭氏力道:
//       结果属性.郭氏力道 = (结果属性.郭氏力道 || 0) + 值
//       break
//     case 属性类型.郭氏根骨:
//       结果属性.郭氏根骨 = (结果属性.郭氏根骨 || 0) + 值
//       break
//     case 属性类型.郭氏身法:
//       结果属性.郭氏身法 = (结果属性.郭氏身法 || 0) + 值
//       break
//     case 属性类型.郭氏元气:
//       结果属性.郭氏元气 = (结果属性.郭氏元气 || 0) + 值
//       break
//     // 无双等级
//     case 属性类型.无双等级:
//       结果属性.最终人物属性.无双等级 += 值
//       break
//     case 属性类型.郭氏无双:
//       结果属性.郭氏无双 += 值
//       break
//     case 属性类型.郭氏额外无双:
//       结果属性.郭氏额外无双等级 += 值
//       break
//     // 破招值
//     case 属性类型.破招值:
//       结果属性.最终人物属性.破招值 += 值
//       break
//     case 属性类型.郭氏破招:
//       结果属性.郭氏破招 += 值
//       break
//     // 全能等级
//     // 全能放最后计算
//     case 属性类型.全能等级:
//       结果属性.最终人物属性.全能等级 += 值
//       // 结果属性.最终人物属性.破招值 += 值
//       // 结果属性.最终人物属性.无双等级 += 值
//       break
//     // 基础攻击
//     case 属性类型.全基础攻击:
//     case 属性类型.外功基础攻击:
//     case 属性类型.内功基础攻击:
//       结果属性.最终人物属性.基础攻击 += 值
//       break
//     case 属性类型.郭氏全基础攻击:
//     case 属性类型.郭氏外功基础攻击:
//     case 属性类型.郭氏内功基础攻击:
//       结果属性.郭氏基础攻击 += 值
//       break
//     // 破防
//     case 属性类型.全破防等级:
//     case 属性类型.外功破防等级:
//     case 属性类型.内功破防等级:
//       结果属性.最终人物属性.破防等级 += 值
//       break
//     case 属性类型.郭氏全破防等级:
//     case 属性类型.郭氏外功破防等级:
//     case 属性类型.郭氏内功破防等级:
//       结果属性.郭氏破防等级 += 值
//       break
//     // 会心
//     case 属性类型.全会心等级:
//     case 属性类型.外功会心等级:
//     case 属性类型.内功会心等级:
//       结果属性.最终人物属性.会心等级 += 值
//       break
//     case 属性类型.全会心百分比:
//     case 属性类型.外功会心百分比:
//     case 属性类型.内功会心百分比:
//       结果属性.额外会心率 += 值
//       break
//     // 会心效果
//     case 属性类型.全会心效果等级:
//     case 属性类型.外功会心效果等级:
//     case 属性类型.内功会心效果等级:
//       结果属性.最终人物属性.会心效果等级 += 值
//       break
//     case 属性类型.郭氏全会心效果等级:
//     case 属性类型.郭氏外功会心效果等级:
//     case 属性类型.郭氏内功会心效果等级:
//       结果属性.郭氏额外会效果值 += 值
//       break
//     case 属性类型.郭氏突破上限会心效果等级:
//       结果属性.郭氏突破上限会心效果值 += 值
//       break
//     // 无视防御相关
//     case 属性类型.内功基础防御:
//     case 属性类型.外功基础防御:
//       结果属性.计算目标 = {
//         ...结果属性.计算目标,
//         防御点数: Math.max(结果属性.计算目标.防御点数 + 值, 0),
//       }
//       break
//     case 属性类型.郭氏外功基础防御:
//     case 属性类型.郭氏内功基础防御:
//       结果属性.郭氏属性无视防御 += 值
//       break
//     case 属性类型.郭氏全无视防御:
//       结果属性.郭氏无视防御 += 值
//       break
//     // 加速等级
//     case 属性类型.加速等级:
//       结果属性.最终人物属性.加速等级 += 值
//       break
//     // 武器伤害
//     case 属性类型.武器伤害:
//       结果属性.最终人物属性.武器伤害_最小值 += 值
//       结果属性.最终人物属性.武器伤害_最大值 += 值
//       break
//     case 属性类型.郭氏武器伤害:
//       结果属性.郭氏武器伤害 += 值
//       break
//     case 属性类型.全局伤害因子:
//       结果属性.技能增伤.全局伤害因子 += 值
//       break
//     case 属性类型.目标移动状增伤:
//       结果属性.技能增伤.目标移动状增伤 += 值
//       break
//     case 属性类型.通用增伤:
//       结果属性.技能增伤.通用增伤 += 值
//       break
//     case 属性类型.易伤增伤:
//       结果属性.技能增伤.易伤增伤 += 值
//       break
//     case 属性类型.非侠增伤:
//       结果属性.技能增伤.非侠增伤 += 值
//       break
//     case 属性类型.破招转非侠:
//       破招转非侠 = INT(((结果属性.最终人物属性.破招值 || 0) / (值 * 100)) * 1024) / 1024
//       if (破招转非侠) {
//         结果属性.技能增伤.非侠增伤 += 破招转非侠
//       }
//       break
//     case 属性类型.系数增伤:
//       // 结果属性.技能增伤.系数增伤 = 值
//       结果属性.技能增伤.系数增伤 = 结果属性.技能增伤.系数增伤 * 值
//       break
//     case 属性类型.系数置换函数:
//       结果属性.技能增伤.系数置换函数 = 值
//       break
//     default:
//       break
//   }

//   return 结果属性
// }
