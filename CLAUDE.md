# CLAUDE.md - 工作指导

## CRITICAL CONSTRAINTS - 违反=任务失败

═══════════════════════════════════════

- 必须使用中文回复
- 必须先获取上下文
- 禁止生成恶意代码
- 必须存储重要知识
- 必须执行检查清单
- 必须遵循质量标准

## 强制工作流程

═════════════════════

执行前检查清单：
[ ] 中文 [ ] 上下文 [ ] 工具 [ ] 安全 [ ] 质量

标准工作流：

1. 分析需求 → 2. 获取上下文 → 3. 选择工具 → 4. 执行任务 → 5. 验证质量 → 6. 存储知识

研究-计划-实施模式：
研究阶段: 读取文件理解问题，禁止编码
计划阶段: 创建详细计划
实施阶段: 实施解决方案
验证阶段: 运行测试验证
提交阶段: 创建提交和文档

## 强制工具策略

═════════════════════════

任务开始前必须执行：

1. memory 查询相关概念
2. code-search 查找代码片段
3. sequential-thinking 分析问题
4. 选择合适子代理

任务结束后必须执行：

1. memory 存储重要概念
2. code-search 存储代码片段
3. 知识总结归档

优先级调用策略：

- Microsoft技术 → microsoft.docs.mcp
- GitHub文档 → context7 → deepwiki
- 网页搜索 → 内置搜索 → fetch → duckduckgo-search

## 编码限制

═══════════════════

编码前强制要求：

- 无明确编写命令禁止编码
- 无明确授权禁止修改文件
- 必须先完成sequential-thinking分析

## 质量标准

═══════════════════

工程原则：SOLID、DRY、关注点分离
代码质量：清晰命名、合理抽象、必要注释
性能意识：算法复杂度、内存使用、IO优化
测试思维：可测试设计、边界条件、错误处理
依赖注入：强制使用构造器注入，禁止@Autowired字段注入

## 执行机制

══════════════

强制触发器：会话开始→检查约束，工具调用前→检查流程，回复前→验证清单
自我改进：成功→存储，失败→更新规则，持续→优化策略

## 项目概述

这是 **JX3 DPS Online** (剑网3配装计算器)，一个基于网页的剑网3游戏伤害计算器。它可以计算不同角色配置、装备搭配和技能循环在多个心法下的 DPS（每秒伤害）。

## 命令

### 开发

```bash
# 安装依赖
yarn  # 或 npm install

# 启动开发服务器（推荐）
yarn run dev

# 启动开发服务器（备选）
yarn run start

# 开发服务器运行在 http://localhost:3000/
```

### 构建

```bash
# 生产环境构建
yarn run build

# 带打包分析器的生产环境构建
yarn run build:analyzer

# 本地构建（用于离线使用）
yarn run build:local

# Windows 本地构建
yarn run build:localWindows

# 构建 getDps worker
yarn run buildGetDps
```

### 测试

```bash
# 运行测试
yarn run test
```

### 数据导入脚本

```bash
# 导入装备数据（外功）
yarn run equip:physics

# 导入装备数据（内功）
yarn run equip:magic

# 导入本地装备数据
yarn run equip

# 导入五彩石数据
yarn run stone

# 导入附魔数据
yarn run enchants

# 导入创意料理数据
yarn run chuangyi

# 导入奇穴数据
yarn run qixue
```

### 代码检查

```bash
# 代码检查和格式化（通过 husky pre-commit hook 运行）
prettier --write <files>
oxlint --fix <files>
```

## 架构

### 核心结构

代码库全面使用**中文目录和文件名**。关键目录：

- **src/心法模块/** - 心法模块 - 每个心法都有自己的计算逻辑
- **src/计算模块/** - DPS 计算的核心引擎
- **src/功能模块/** - 功能模块（设置、结果展示、系统工具）
- **src/数据/** - 静态游戏数据（装备、附魔、五彩石、增益）
- **src/页面/** - 页面组件（登录、计算器、加速计算）
- **src/组件/** - 可复用的 UI 组件
- **src/工具函数/** - 工具函数

### 状态管理

使用 **Redux Toolkit**，包含两个主要 slice：

- `data` - 游戏数据状态（装备、角色属性等）
- `system` - 系统状态（UI 设置、选中的心法等）

Store 配置：`src/store/index.ts`

### 路由

三个主要路由：

- `/` - 登录页面
- `/dps` - 主计算器 - 受保护路由
- `/haste` - 加速计算器

入口文件：`src/index.tsx`

### 心法模块系统

每个心法都是 `src/心法模块/心法/[心法名称]/` 中的独立模块：

```
心法模块/心法/[心法名称]/
├── index.ts              # 模块入口
├── 技能系数.ts           # 技能系数
├── 技能增益/             # 技能增益/buff
├── 计算循环/             # 计算循环/技能循环
├── 默认数据/             # 默认数据
├── 奇穴/                 # 奇穴
└── 资源/                 # 资源（图标等）
```

可用的心法模块包括：山海心诀、北傲诀、毒经、分山劲、焚影圣诀、孤锋诀、花间游、凌海诀、太玄经、天罗诡道、无方、笑尘诀、易筋经、幽罗引、周天功、紫霞功及其变体。

### 计算引擎

位于 `src/计算模块/`：

- **郭氏计算/** - 郭氏算法（主要计算方法）
- **非郭氏计算/** - 非郭氏算法（备选计算方法）
- **dps-kernel-optimizer/** - DPS 优化算法
- **统一工具函数/** - 统一工具函数
- **职业特殊处理/** - 职业特殊处理
- **循环秒伤计算.ts** - 循环 DPS 计算
- **计算函数.ts** - 核心计算函数

### 构建配置

- **基于 Webpack** 的构建系统（自定义配置在 `config/webpack.config.js`）
- **TypeScript** 配置路径别名（`@/*` 映射到 `src/*`）
- **Babel** 用于转译
- **Less** 用于样式
- **React 18** 配合 React Router v6

### 特殊构建模式

- `REACT_APP_BUILD_TYPE=local` - 用于离线/本地使用的构建（跳过登录，使用相对路径）
- `BUILD_ANALYZER=true` - 启用 webpack 打包分析器
- `GET_DPS=true` - 构建 getDps worker 脚本

## 添加新心法模块

要添加新的心法：

1. 在 `src/心法模块/心法/[新心法名称]/` 中创建目录结构
2. 实现必需的文件：`index.ts`、`技能系数.ts`、技能增益、计算循环、默认数据、奇穴
3. 在 `src/心法模块/心法/index.ts` 中注册该模块
4. 如需要，在 `src/数据/` 中添加相应的数据文件

参考现有的心法模块（如 `山海心诀`）了解实现模式。

## 关键技术细节

- **路径别名**：`@/` 解析到 `src/`
- **模块解析**：使用 Node 解析策略
- **严格 TypeScript**：已启用，但 `noImplicitAny: false`
- **目标**：ES5 以实现广泛兼容性
- **JSX**：保留（由 Babel 处理）
- **代码检查**：使用 oxlint（基于 Rust 的快速检查器）+ Prettier
- **Git hooks**：Husky + lint-staged 用于 pre-commit 格式化

## 数据文件

根目录的 JSON 文件包含游戏数据映射：

- `地图ID映射.json` - 地图 ID 映射
- `五彩石映射.json` - 五彩石映射
- `装备来源映射.json` - 装备来源映射

## 本地构建说明

使用本地/离线构建（`build:local`）时，应用会：

- 跳过登录流程
- 使用相对路径（`PUBLIC_URL=./`）
- 直接渲染计算器，不使用路由
