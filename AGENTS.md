# 项目 Agent 经验记录

## 问水诀 Excel 对齐：技能统计与图表排序

- **触发信号**：问水诀斩岳循环的技能占比、技能数量或图表顺序与 Excel `配装计算器!M27:O33` 不一致，尤其出现“夕照雷峰”拆成多行、占比高的技能显示在下方。
- **根因 / 约束**：Excel 技能占比表不是直接看单个伤害行，而是来自 `伤害与收益计算!BI10:BK16`，其数据源为 `BB:BE` 汇总表。项目的“合并同名技能”需要按 `统计名称 || 技能名称` 合并，不能只合并设置了 `统计名称` 的技能。
- **正确做法**：核对 Excel 时优先读取 `配装计算器!M27:O33` 与 `伤害与收益计算!BB:BE` 汇总；问水诀中云飞类技能用统一 `统计名称: '云飞玉皇'`，同名多段技能依赖统计工具合并。
- **图表注意点**：G2 横向柱图 `coordinate().transpose()` 会把类目视觉顺序上下反转；若数据已按技能输出降序排列，图表需要使用 `coordinate().transpose().reflect('y')` 才能从上到下显示降序。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，并至少确认前 7 项顺序为 `云飞玉皇、夕照雷峰、断潮、风来吴山、破、九皋鹤野·落剑、昆吾·弦刃`，夕照雷峰合并后数量为 `83`。
- **适用范围**：问水诀循环数据、技能统计弹窗、技能统计图表，以及后续从 Excel 搬运同类 DPS/技能占比逻辑的排查。

## 问水诀 Excel 对齐：增益开启后 DPS 差几十万

- **触发信号**：Excel 和项目都开启增益后，问水诀斩岳循环 DPS 相差几十万；无增益或技能占比看起来基本正常。
- **根因 / 约束**：不能只看“增益已开启”，必须逐项对 `配装计算器!R6:T20/U5:V9`、`额外属性!7:8` 和 `伤害与收益计算` 明细行。已确认的关键项包括：`寒啸千军` 是无双百分比 `51 * 覆盖率`，不是无双等级；该增益不可叠层，配置层数必须为 `1`，否则通用公式会再次乘层数并把结果放大几十倍；没有层数选项的增益应以团队增益定义中的标准层数计算，避免旧缓存错误层数继续生效。`号令三军` 每层按 `826`；`凌雪阵` 五重覆盖率按 `55%`；飘黄在问水 Excel 中对应 `逐云寒蕊` 48 次静态真实伤害；斩岳循环断潮数量为 `67.3326 / 3.6396 / 6 / 10 / 52`。
- **正确做法**：优先用 `开荒拉满` 快捷配置复现 Excel 当前增益，阵眼使用 `凌雪阵`，创意料理和香水使用破防项，朝圣、飘黄、号令、破甲、仙王覆盖率按 Excel 当前单元格设置。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环开荒拉满增益对齐 Excel 期望值` 通过，DPS 应接近 `13,126,972.538`，断潮数量为 `138.9722`，逐云寒蕊数量为 `48`。
- **适用范围**：问水诀增益快捷配置、通用团队增益、外功阵眼、飘黄真实伤害、斩岳循环 JSON。

## 问水诀 Excel 对齐：换暗器后 DPS 差异变大

- **触发信号**：Excel 和项目同时更换暗器后，DPS 差异反而扩大；技能占比里 `断潮` 数量和 Excel `配装计算器!O27:O41` / `伤害与收益计算!BB:BE` 不一致。
- **根因 / 约束**：斩岳循环的多段 `断潮` 数量不是静态值，Excel 在 `技能数计算` 中按当前实战会心率重算，公式形态为 `基础数量 * MIN(1, 会心率 + 固定会心率)`。更换暗器会改变会心/会效/无双分布，Excel 的 `断潮` 会随之变化；项目若沿用旧静态断潮次数，会把差异放大。
- **正确做法**：循环 JSON 中保留可读的静态数量，同时用 `动态技能数量` 描述会心触发型技能；统一计算时在团队增益和装备增益面板成型后、追加装备触发技能前，按当前会心率覆盖 `技能数量` 和对应 `增益技能数`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环断潮数量随当前会心率动态变化` 通过；若按 Excel 试炼鞋子 `10/21` 口径，当前暗器场景下 `断潮` 接近 `133.9664`；按项目全局试炼鞋子 `50%` 口径时应接近 `134.0252`。
- **适用范围**：问水诀斩岳循环、后续所有从 Excel 搬运的会随面板属性变化的循环技能数量。

## 问水诀 Excel 对齐：试炼鞋子以项目全局口径为准

- **触发信号**：Excel 中 `试炼鞋子会心` 按 `6000 * 10 / 21 = 2857.142857` 进入面板，但项目全局装备增益按描述“持续 10 秒、每 20 秒最多触发一次”折算为 `6000 * 0.5 = 3000`；因此问水诀最终会心、断潮动态数量、DPS 与 Excel 低幅偏差。
- **根因 / 约束**：全项目其他心法没有在循环 JSON 中额外覆盖 `试炼鞋子会心/破防`，都走 `src/数据/装备/装备增益数据/index.ts` 的全局 `0.5` 口径。Excel 的 `10/21` 属于表格自身口径，不应继续作为项目计算依据。
- **正确做法**：不要在问水诀循环 `覆盖率` 中配置 `试炼鞋子会心: 0.9523809523809523`；斩岳、碧归都统一使用全局 `50%` 覆盖率。若后续做 Excel 差异报告，要把这项标记为“Excel 口径差异”，不要修回项目逻辑。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环最新暗器配置按项目试炼鞋子口径计算` 和 `叠锋意斩岳循环高破防加速酒方案扣除逐云后按项目试炼鞋子口径计算` 通过。
- **适用范围**：问水诀斩岳/碧归循环、动态断潮数量、试炼鞋子会心/破防、后续 Excel 对齐排查。

## 问水诀 Excel 对齐：换多件装备后 DPS 又偏

- **触发信号**：单套装备已对齐 Excel，但更换 2 件以上装备后 DPS 又出现明显差异；尤其装备包含副本精简常驻特效、风特效腰坠等装备特效时差异扩大。
- **根因 / 约束**：`装备增益` 里的旧字段只能表达“数量”或单个“等级”，不能表达同类副本精简多件混搭时的每件档位。例如 `特效_40803: 2` 可能被误算成“二档两层”，但真实可能是 `[3, 2]` 两件分别累加。旧 localStorage 还可能保留错误汇总值，计算入口不能只信任汇总字段。
- **正确做法**：从 `装备列表` 反查装备库补齐装备特效真实档位：副本精简可叠加特效写入 `*_等级列表`，风特效腰坠写入 `风特效腰坠_实际等级`。DPS 计算入口和配装器生成装备信息时都应执行补齐，增益计算优先使用补齐字段，旧字段只作兼容兜底。
- **验证方式**：运行 `npx jest --runTestsByPath src/数据/数据工具/获取装备特效增益数据.test.ts src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认副本精简 `[3, 2]` 累加为 `1928 + 1650`，且 `旧 localStorage 装备增益会按装备列表补齐特效真实档位` 通过。
- **适用范围**：装备增益聚合、问水诀 DPS、配装器换装 DPS 对比，以及后续所有依赖副本精简常驻特效或风特效腰坠的心法。

## 问水诀 Excel 对齐：巽护腕与试炼暗器档位

- **触发信号**：问水诀斩岳循环换上 `特效_40788` 护腕和 38100 试炼暗器后，Excel 为 `13,423,492.834`，项目约低 `8.2w DPS`；或技能统计里没有 `巽`，以及 `无修·荒` 伤害和暗器档位不符。
- **根因 / 约束**：Excel 中 `特效_40788` 的统计名是 `巽`，数量来自 `伤害与收益计算!AC114 = 19.830626450116`，不是通用追加逻辑的 `floor(战斗时间/10)`。同一套配置里 38100 暗器对应 `无修·荒` 单次 `898344`，41400 暗器才是 `976145`。`特效_40788` 自身也按副本品级分 1/2/3 档：35300 护腕应为 `巽·2 = 601965`，41400 护腕应为 `巽·3 = 1138846`；旧 localStorage 若只保存 `特效_40788: 3`，必须从装备列表反查真实档位。
- **正确做法**：问水诀循环 JSON 可保留 `特效·腕` 静态行用于技能数量对齐，但最终循环计算时必须按 `装备增益.特效_40788` 替换成 `巽·1/2/3`，技能系数复用公共真实伤害表并用 `统计名称: '巽'` 合并展示。`补齐装备特效等级` 要从装备列表反查 `特效_40788` 的实际档位，避免 35300 和 41400 共用同一个伤害。`无修·荒` 通过 `技能等级数据` 按 `试炼暗器特效` 的 7/8 档切换真实伤害。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环巽护腕暗器配置对齐 Excel 期望值`、`旧 localStorage 装备增益会按装备列表补齐特效真实档位`、`问水诀静态巽护腕按装备品级切换真实伤害` 通过，并断言 35300 护腕补为 2 档、41400 护腕补为 3 档。
- **适用范围**：问水诀斩岳/碧归循环、装备触发技能统计、试炼暗器档位、`特效_40788` 护腕。

## 问水诀 Excel 对齐：大橙武循环与神兵统计

- **触发信号**：两边武器换成大橙武后，项目 DPS 和技能数量同时不一致；Excel 的 `技能数计算!AS4` 显示 `橙武`，但项目仍显示普通 `叠锋意斩岳循环` / `叠锋意碧归循环`。
- **根因 / 约束**：Excel 会在 `技能数计算!AN11>0` 且循环名包含叠峰意斩岳/碧归时，把 `AS4` 拼成 `橙武` 并切到同名橙武列，例如 `BI/BJ = 叠峰意碧归循环橙武1/2`。项目原先只按循环名匹配，不看 `装备增益.大橙武特效`，因此橙武装备仍可能使用普通循环。另一个易错点是 Excel 技能占比把 `神兵·云飞玉皇`、`神兵·听雷` 单独统计，不并入普通 `云飞玉皇` / `听雷`。
- **正确做法**：`use-cycle` 在存在 `装备增益.大橙武特效` 时，应优先匹配同名 `橙武` 循环；问水诀需注册 `叠锋意斩岳循环橙武` 和 `叠锋意碧归循环橙武`。神兵技能系数不要设置普通技能的 `统计名称`，保持技能占比与 Excel `伤害与收益计算!BI:BK` 一致。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武装备自动匹配 Excel 斩岳橙武循环` 和 `大橙武装备自动匹配 Excel 碧归橙武循环且神兵技能单独统计` 通过；当前碧归橙武缓存里 `云飞玉皇 = 154`、`神兵·云飞玉皇 = 23.0849609375`、`玉山揽云 = 23`。
- **适用范围**：问水诀大橙武装备、循环选择、技能统计、技能占比图表，以及后续 Excel 橙武分支搬运。

## 问水诀 Excel 对齐：技能增益不再带手法伤害

- **触发信号**：排查 `神兵·云飞玉皇` 时，Excel `伤害与收益计算!P87` 包含 `怜光 102/1024` 和 `手法 8/1024`，但项目口径要求“把怜光加上，手法去掉”。
- **根因 / 约束**：问水诀此前通过 `问水技能增益` 给轻剑、重剑和破招类技能批量加了 `手法伤害`，导致所有使用该 helper 的技能都多吃 `8/1024` 通用增伤；`神兵·云飞玉皇` 是单独技能系数，原先只吃通用增益，不吃 `怜光`。
- **正确做法**：`问水技能增益` 只保留通用增益和秘籍增益，不再接入 `手法伤害`；`问水重剑技能增益` 继续在此基础上追加 `怜光`；`神兵·云飞玉皇` 单独配置为 `通用增益 + 怜光`，但 `神兵·听雷` 保持通用增益。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `问水诀技能不再使用手法伤害且神兵云飞吃怜光` 通过，并确认全问水诀源码只有该测试会出现 `手法伤害` 字符串。
- **适用范围**：问水诀技能系数、神兵技能、破招伤害、后续 Excel DPS 差异排查。

## 问水诀 Excel 对齐：紫武 210s 技能数与去手法 DPS 口径

- **触发信号**：紫武斩岳 `210s` 且 `技能数计算!AS4=低延迟` 时，项目技能统计出现 `风来吴山=17`、`云飞玉皇=101`、`破=127`，而 Excel `伤害与收益计算!BI:BK` 为 `风来吴山=48`、`云飞玉皇=118`、`破=142`。
- **根因 / 约束**：`问水诀_叠锋意斩岳循环低延迟` 的 `210s` 技能数量列表曾按错误行序搬运，和当前斩岳循环 JSON 的技能详情索引不一致。技能数修正后，项目 DPS 仍会比 Excel `14,508,835` 低约 `8.7w`，这是因为 Excel 重剑 `P` 增伤仍包含 `手法 8/1024`，而项目按既定口径不再接入手法。
- **正确做法**：核对紫武 210s 时，以 Excel `伤害与收益计算!BI:BK` 校验技能数；DPS 对齐时必须先判断差值是否正好来自 `8/1024` 手法增伤，不要为了追 Excel 当前数值把 `手法伤害` 加回 `问水技能增益`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环紫武 210s 加速酒方案对齐 Excel 技能数并保持去手法 DPS 口径` 通过；该用例应断言 `风来吴山=48`、`云飞玉皇=118`、`断潮≈137.7296`，DPS 约 `14,421,729`。
- **适用范围**：问水诀紫武斩岳低延迟 210s、战斗时间技能数表、后续 Excel DPS 差异排查。

## 问水诀 Excel 对齐：飘黄使用通用逐云寒蕊

- **触发信号**：问水诀开启飘黄后技能统计出现 `逐云含蕊`，或问水诀 `技能系数.ts` 单独定义了 `逐云含蕊`。
- **根因 / 约束**：`逐云含蕊` 是旧搬运误写，正确技能名与其他心法一致，均为通用 `逐云寒蕊`。飘黄追加技能不应因问水诀单独命名而分叉。
- **正确做法**：问水诀不要单独定义 `逐云含蕊`；只从通用真实伤害系数中引入 `逐云寒蕊` 这一条，避免整包引入其它通用真实伤害并影响装备触发伤害。`循环秒伤计算.ts` 中飘黄追加固定使用 `逐云寒蕊`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `问水诀飘黄使用通用逐云寒蕊技能名` 通过，且源码中 `逐云含蕊` 只作为“错误名不存在”的测试断言出现。
- **适用范围**：问水诀飘黄/逐云寒蕊、真实伤害技能增益口径、后续与通用飘黄技能的差异排查。

## 问水诀 Excel 对齐：旧缓存斩岳换橙武后少碧归奇穴

- **触发信号**：localStorage 仍保存 `当前计算循环名称: 叠锋意斩岳循环` 和奇穴 `斩岳`，但装备已有 `大橙武特效`；Excel `配装计算器!F22/AB31` 为 `16,703,691`，项目约 `16,294,237`，技能数量基本能对上但 DPS 少约 `409,454`。
- **根因 / 约束**：`use-cycle` 会按武器限制从旧斩岳缓存回退到实际 `叠锋意碧归循环`，但秒伤入口若提前使用 state 里的旧奇穴，`技能增益/通用增益.ts` 中 `碧归` 的 `307/1024` 通用增伤不会启用。
- **正确做法**：计算入口应在 `use-cycle` 之后判断缓存循环名和实际循环名是否不同；只有发生循环回退时，奇穴才跟随实际循环，正常同名循环仍保留用户当前奇穴或调用方传入的 `更新奇穴数据`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武旧缓存仍保存斩岳循环和奇穴时按实际碧归循环对齐 Excel` 通过。
- **适用范围**：问水诀大橙武切换、循环武器限制、旧 localStorage 兼容、秒伤计算入口的奇穴选择。

## 问水诀 Excel 对齐：碧归断潮也需要动态数量

- **触发信号**：Excel 当前碧归橙武场景 `配装计算器!K30=220s` 且 `F22/AB31` 为 `7,844,254`，项目技能汇总中 `断潮`、`云飞玉皇`、`玉山揽云` 等数量仍停留在旧 210s 导入值。
- **根因 / 约束**：断潮触发数量动态规则不只适用于斩岳循环。220s 碧归循环五段断潮基础数为 `76 / 4 / 11 / 7 / 55`，前两段按 `会心率 + 0.3`，后三段分别按 `会心率 + 0.4 / 0.5 / 0.6` 后封顶计算。只改 `战斗时间` 或只给斩岳 JSON 补动态数量，都会让碧归继续沿用旧技能数。
- **正确做法**：碧归循环 JSON 的五段 `断潮` 都保留原始基础数量用于可读性，同时补 `动态技能数量: { 计算类型: '会心触发', 基础数量, 固定会心率 }`，让统一计算在装备和团队增益面板成型后覆盖 `技能数量` 与对应 `增益技能数`。当前 Excel 碧归橙武 220s 核心展示数量应为 `云飞玉皇 164`、`破 149`、`夕照雷峰 67`、`断潮 147.6`、`风来吴山 48`、`九皋鹤野·落剑 65`、`玉山揽云 25`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武装备自动匹配 Excel 碧归橙武循环且神兵技能单独统计` 和 `叠锋意碧归循环断潮数量随当前会心率动态变化` 通过。
- **适用范围**：问水诀碧归循环、碧归橙武循环、技能统计、DPS 与 Excel 对齐排查。

## 问水诀 Excel 对齐：碧归橙武 DPS 与技能数同时偏

- **触发信号**：用户 localStorage 为 `叠锋意碧归循环橙武`，Excel `配装计算器!F22/AB31=7,844,254`，项目约为 `7,897,915`；关键技能数量看似接近，但 DPS 仍差约 `5.3w`。
- **根因 / 约束**：当前 Excel `伤害与收益计算!AD8=AD7/(MAX(技能数计算!A2:A515))`，实际计算器战斗时间应按技能轴最大时间 `219.51` 对齐，而不是只把 DPS 分母单独设为 `219.51`。`逐云寒蕊` 属于 `飘黄` 团队增益追加技能，未启用增益时不应出现在基础循环中，也不应参与技能统计或 DPS。`夕照雷峰` 明细 `P` 比项目多 `51/1024`，对应 `5%伤害` 秘籍，当前碧归 Excel 口径为 `5%/4%/3%` 三本伤害秘籍。
- **正确做法**：碧归橙武循环详情直接使用 `战斗时间: 219.51`，不要再额外配置 `秒伤计算时间`；基础循环不要放 `逐云寒蕊` 占位行，`逐云寒蕊` 只在 `增益启用` 且 `飘黄` 启用时由通用增益逻辑追加。循环详情可用 `秘籍` 覆盖 Excel 口径，碧归当前应覆盖 `夕照雷峰: ['5%伤害', '4%伤害', '3%伤害']`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武装备自动匹配 Excel 碧归橙武循环且神兵技能单独统计` 通过，DPS 接近 `7,844,254`，并确认无增益时 `逐云寒蕊` 不出现在技能统计中。
- **适用范围**：问水诀碧归循环、碧归橙武循环、Excel DPS 分母、飘黄/逐云寒蕊统计、夕照雷峰秘籍口径。

## 问水诀 Excel 对齐：当前碧归增益覆盖率按 220s 重算

- **触发信号**：当前碧归橙武开启 `开荒拉满` 后，Excel DPS 为 `15,736,658.691`，项目仍高到约 `15,753,719` 或 `15,774,106`；普通外功技能整体高约 `0.3008%`，但 `刃凌` 等真实伤害能对上。
- **根因 / 约束**：差值来自覆盖率口径混用。Excel 当前碧归 `技能数计算!AS1=220`，`风特效腰坠` 覆盖率是 `30/220 = 0.136363636363636`，`号令三军` 覆盖率是 `45/220 = 20.4545454545455%`；旧斩岳用例仍依赖 `30/210` 和 `45/210`。不能把 `开荒拉满` 全局默认覆盖率改成 220s 口径，否则会打破旧斩岳增益测试。
- **正确做法**：用循环详情的 `覆盖率` 覆盖装备特效，用 `增益覆盖率` 覆盖团队增益；当前碧归写入 `风特效腰坠: 0.136363636363636` 和 `增益覆盖率.号令三军: 20.4545454545455`。`飘黄` 次数也要分口径：未声明 `技能数量计算时间` 的旧循环保持原公式 `floor(战斗时间 / 50) * 12 = 48`，当前碧归显式声明 `技能数量计算时间: 220` 后按 Excel 公式 `12*(INT((AS1-10)/51.5)+1)=60`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武碧归循环开荒拉满增益对齐 Excel 期望值`、旧斩岳开荒拉满和最新暗器测试同时通过。
- **适用范围**：问水诀碧归增益、风特效腰坠覆盖率、号令三军覆盖率、飘黄/逐云寒蕊次数，以及后续同一快捷增益在不同 Excel 循环时间下复用的场景。

## 增益面板：加速酒不改变面板或循环档位

- **触发信号**：启用增益后，更换家园酒品为 `女儿红·旬又三（加速）` 等加速酒，角色属性里的加速等级没有增加，`useCycle` 仍按旧装备加速选择循环，导致强制加速循环匹配错误。
- **根因 / 约束**：部分工具模块在心法数据初始化前，于模块顶层解构 `获取当前数据()`，例如 `const { 小药小吃 } = 获取当前数据()`。如果模块早于 `初始化心法数据` 加载，后续函数会一直持有 `undefined` 或空数组，导致 `计算增益数据中加速值`、`根据增益选项获取增益集合` 等无法读到当前心法的小药小吃。
- **正确做法**：计算工具内不要顶层缓存当前心法数据；需要 `小药小吃`、`团队增益`、`阵眼`、`装备增益数据`、`基础GCD` 等时，在函数执行时动态调用 `获取当前数据()`。面板展示工具和循环选择工具要共用同一套动态读取结果。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `加速酒工具在心法数据初始化后动态读取小药小吃`、`增益集合工具在心法数据初始化后动态读取小药小吃`、`启用加速酒后循环选择使用增益后的加速值`、`角色面板工具在启用增益后展示加速酒加成` 通过。
- **适用范围**：`src/工具函数/data.ts`、`src/计算模块/统一工具函数/增益计算函数.ts`、`src/功能模块/基础设置/面板信息/工具.ts`，以及后续任何依赖当前心法数据的通用工具函数。

## 问水诀 Excel 对齐：加速酒换装后斩岳偏低/偏高

- **触发信号**：用户装备加速不足但启用 `女儿红·旬又三（加速）` 后，Excel 斩岳 DPS 为 `13,259,838`，项目初始约 `12,577,540`；修正循环后又出现普通技能整体高约 `0.3008%`。
- **根因 / 约束**：当前 Excel `叠峰意斩岳循环低延迟` 是高加速档单独列，技能数量不是旧低加速斩岳明细；DPS 分母来自 `MAX(技能数计算!A2:A515)=218.93`。该列还使用 `风特效腰坠=30/220`、`号令三军=45/220` 的覆盖率。用户 localStorage 可能只保存 `夕照雷峰` 秘籍，计算入口不能因此丢掉默认秘籍；循环详情可以按 Excel 列覆盖秘籍。
- **正确做法**：斩岳循环拆成低/高加速两个 `循环详情`，高加速详情使用 `秒伤计算时间: 218.93`、`技能数量计算时间: 220`、`覆盖率.风特效腰坠: 0.136363636363636`、`增益覆盖率.号令三军: 20.4545454545455`。`秒伤计算` 合并秘籍时按 `默认秘籍 -> 方案秘籍 -> 循环详情秘籍`，避免方案只保存局部秘籍导致其它技能失去默认秘籍。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `叠锋意斩岳循环加速酒换装方案内秘籍补齐后对齐 Excel 期望值` 通过，DPS 接近 `13,259,838`，技能数量为 `云飞玉皇 124`、`夕照雷峰 86`、`断潮 152`、`破 148`、`逐云寒蕊 60`。
- **适用范围**：问水诀斩岳循环、加速酒换档、方案秘籍保存、Excel 低延迟列、风特效腰坠和号令三军覆盖率。

## 问水诀 Excel 对齐：战斗时间不能只改分母

- **触发信号**：在 `配装计算器!K30` 和项目基础设置同时切换战斗时间后，DPS 差异明显扩大；项目技能数量仍像 220s，而 Excel 已按新时间重算。
- **根因 / 约束**：Excel 的战斗时间下拉会改变 `技能数计算!AS1`，进而重算技能时间轴和 `伤害与收益计算` 汇总；项目循环 JSON 目前保存的是特定时间口径的汇总技能数量，不包含各下拉时间对应的完整技能数。只覆盖 `战斗时间`、`秒伤计算时间` 或 `技能数量计算时间` 会把 220s 技能数套到其它分母上，结果必然失真。
- **正确做法**：只有循环详情显式声明 `支持战斗时间覆盖` 且能同步提供对应技能数量时，才允许方案战斗时间参与 DPS 计算。未接入对应技能数的 Excel 下拉项可以展示，但必须禁用或忽略旧缓存，避免错误 DPS。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `未声明支持战斗时间覆盖的循环会忽略方案战斗时间` 通过。
- **适用范围**：基础设置战斗时间下拉、秒伤计算入口、问水诀循环 JSON，以及后续按 Excel `210s/240s/300s/∞` 等时间口径扩展循环数据的实现。

## 问水诀 Excel 对齐：无穷战斗时间有独立明细口径

- **触发信号**：战斗时间选择 `∞` 后，项目和 Excel 的 DPS 差异明显大于有限时间；常见表现是 `夕照雷峰`、`断潮`、`破` 数量各少 1，`逐云寒蕊` 仍按有限时间公式算成整数。
- **根因 / 约束**：Excel `K30="∞"` 会切到 `无穷技能数计算` / `无穷伤害与收益计算`。无穷表不是有限时间缩放表，`逐云寒蕊` 有独立非整数次数；同时 `刃凌` 这类 `会心间隔触发` 技能的 `199.8` 是动态计算基准时间，不是最终技能次数，不能直接替换成 Excel 汇总次数再让动态函数二次折算。
- **正确做法**：`问水诀Excel战斗时间数据` 的 `∞` 明细必须逐行核对 `无穷伤害与收益计算!E/AC`；基础循环不放 `逐云寒蕊`，由 `追加技能数量` 覆盖飘黄追加次数；`刃凌` 保留动态基准时间，其最终次数由 `动态技能数量` 计算；风特效腰坠、号令三军这类覆盖率也要允许在 `∞` 数据项里显式覆盖，不能统一按 `无穷技能数计算!AS1` 重算。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武旧缓存选择无穷战斗时间时按 Excel 无穷表计算` 和 `问水诀无穷战斗时间选项按 Excel 无穷表重算技能数量` 通过。
- **适用范围**：问水诀战斗时间 `∞`、碧归橙武循环、飘黄/逐云寒蕊、刃凌动态次数、后续从 Excel 无穷表搬运其它循环数据。

## 问水诀 Excel 对齐：逐云寒蕊真实伤害按装分动态计算

- **触发信号**：`∞` 碧归橙武场景技能数量已对齐，但 DPS 仍低一万以上；明细里 `逐云寒蕊` 数量正确，单次真实伤害却低于 Excel `无穷伤害与收益计算!AB82`。
- **根因 / 约束**：`逐云寒蕊` 是通用飘黄追加技能，真实伤害基础值应按当前装分动态计算。Excel 公式会先对 `0.0080170644 * 装分 - 1388.323959` 取整，再乘层数与固定系数，最终值保留小数。
- **正确做法**：`计算前校验技能属性` 中让 `逐云寒蕊` 复用 `获取逐云寒蕊真实伤害`；该工具函数按 Excel 顺序先 `INT` 基础值、不要对最终结果再取整。默认配装若没有完整装备列表，也必须提供与 Excel 逐云口径一致的 `装分`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认 `大橙武旧缓存选择无穷战斗时间时按 Excel 无穷表计算` 中 `逐云寒蕊` 总伤接近 `18,495,582.233`，且旧斩岳开荒拉满用例不回退。
- **适用范围**：问水诀飘黄/逐云寒蕊、通用逐云寒蕊真实伤害公式、默认配装装分，以及后续所有按 Excel 装分计算飘黄真实伤害的场景。

## 问水诀 Excel 对齐：无穷时长号令三军覆盖率为 0

- **触发信号**：`∞` 碧归橙武且开启 `开荒拉满` 时，项目 DPS 比 Excel 高几十万；用户高破招橙武旧缓存示例中项目约 `15,940,168`，Excel `配装计算器!F22/AB31=15,219,327`。
- **根因 / 约束**：有限时长碧归橙武使用 `号令三军=45/220`，但 Excel 无穷表对应 `额外属性!BH100=0`。把有限时长覆盖率复用到 `∞` 会额外增加 `826 * 20.4545%` 无双等级，导致普通技能整体偏高。
- **正确做法**：`问水诀_叠锋意碧归循环橙武["∞"]` 必须显式设置 `增益覆盖率.号令三军 = 0`；有限时长继续使用 `45/220`。排查剩余小幅差异时不要为了贴 Excel 原表重新加入已按项目口径移除的 `手法` 增伤。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand -t "无穷|用户高破招"`，确认用户高破招橙武旧缓存 `∞` 场景按项目无手法口径约为 `15,169,969.794`，不再回到 `15,940,168`。
- **适用范围**：问水诀 `∞` 战斗时间、碧归橙武循环、`开荒拉满` 增益覆盖率、旧 localStorage 从斩岳回退碧归橙武的场景。

## 问水诀 Excel 对齐：紫武低延迟各时长必须同步技能轴

- **触发信号**：紫武斩岳装备启用加速酒后，切换 `240s/300s/360s` 等有限战斗时间时，项目 DPS 和 Excel 相差数百万；技能统计里 `云飞玉皇`、`夕照雷峰`、`破`、`九皋鹤野·落剑` 等数量也明显偏少。
- **根因 / 约束**：Excel 高加速紫武使用 `技能数计算!AY/AZ` 的 `叠峰意斩岳循环低延迟` 时间轴，不是把默认 220s 明细按分母缩放。`问水诀Excel战斗时间数据` 若只改 `秒伤计算时间`，或沿用旧技能数量列表，会同时造成 DPS 分母和总伤技能数错位。
- **正确做法**：`问水诀_叠锋意斩岳循环低延迟` 的每个有限时长都要逐行按 Excel 时间轴更新 `技能数量列表`，并用 `追加技能数量.逐云寒蕊` 覆盖飘黄次数。当前 240s 应为 `秒伤计算时间=239.96`、`云飞玉皇=134`、`夕照雷峰=95`、`破=162`、`逐云寒蕊=60`。
- **验证方式**：运行 `npx jest --runTestsByPath src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand -t "紫武低延迟各时长"`，确认 `240/300/310/360/420/480/540/600` 的关键技能数都按 Excel 时间轴重算。
- **适用范围**：问水诀紫武斩岳循环、加速酒高加速档、基础设置战斗时间下拉、有限时长 DPS 与技能统计。

## 问水诀大附魔：普通和英雄必须走公共口径

- **触发信号**：大附魔选择普通/英雄后，问水诀 DPS 或技能统计数值与其他门派表现不一致；尤其 `昆吾·弦刃`、`刃凌` 的单次真实伤害看起来一直是英雄数值，或腰带大附魔快照增益没有生效。
- **根因 / 约束**：其他门派通常复用 `src/心法模块/统一数据/真实伤害系数.ts` 和 `获取快照增益()`，其中腕部/鞋子大附魔分别存在普通与英雄技能名：`昆吾·弦刃` / `昆吾·弦刃·英雄`、`刃凌` / `刃凌·英雄`。问水诀曾在本地技能系数里把英雄真实伤害写到普通技能名下，并且通用增益缺少公共快照增益，导致和公共模块分叉。
- **正确做法**：问水诀不要本地硬编码腕鞋大附魔真实伤害，直接从公共真实伤害表取四个技能，并给它们接入问水诀通用增益。问水诀通用增益必须包含 `...获取快照增益()`。循环里已有静态 `昆吾·弦刃` / `刃凌` 时，`根据增益修改最终循环` 需要按 `装备增益.大附魔_伤腕/伤鞋` 把技能名替换为普通或英雄档位，避免英雄装备继续按普通名查系数。
- **验证方式**：运行 `npx jest --runTestsByPath src/计算模块/统一工具函数/增益计算函数.test.ts src/数据/数据工具/获取装备特效增益数据.test.ts src/心法模块/心法/问水诀/问水诀dps.test.ts --watchAll=false --runInBand`，确认问水诀大附魔普通/英雄系数、公共快照增益和静态循环技能名切换都通过。
- **适用范围**：问水诀技能系数、通用增益、腕部/鞋子/腰带大附魔、技能统计展示、DPS 与其他门派公共装备增益对齐。

## 配装器大附魔：品级限制只检查装备上限

- **触发信号**：高品装备仍能选择超过适用范围的大附魔，或低品装备无法选择英雄大附魔；通过“一键大附魔”、旧缓存或导入数据还能写入超过装备品级上限的档位。
- **根因 / 约束**：大附魔描述的是“可用于不超过某品级的装备”，不是普通/英雄各自的装备品级区间。当前赛季普通大附魔最大装备品级为 `35900`，英雄大附魔最大装备品级为 `43000`；因此 `35300` 等低品装备两档都能使用，`41400` 只能使用英雄档，边界值包含在内。
- **正确做法**：公共常量只记录普通/英雄各自的最大装备品级，判断函数只校验 `装备品级 <= 最大装备品级`；下拉、装备切换、一键大附魔和 `根据表单选项获取装备信息` 必须复用同一清理函数。`本赛季英雄品级` 是装备分类阈值，不能拿来当英雄大附魔的最低适用品级。
- **验证方式**：运行 `npx jest --runTestsByPath src/功能模块/基础设置/属性录入/配装器/功能组件/大附魔选择/index.test.tsx src/功能模块/基础设置/属性录入/配装器/工具函数/根据表单选项获取装备信息/index.test.ts --watchAll=false --runInBand`，确认 `35300` 和 `35900` 均显示普通/英雄两档，`41400` 只显示英雄，超过 `43000` 的英雄大附魔会被清除；再运行 `npm run build`。
- **适用范围**：配装器大附魔下拉、一键大附魔、装备切换、配装导入、旧缓存兼容、装备信息计算入口，以及后续赛季大附魔 ID / 品级范围更新。

## 线上发布与 HTTPS：wenshuijue.top

- **触发信号**：`www.wenshuijue.top` 只能通过 HTTP 访问，或本地构建已更新但线上 `index.html` 仍引用旧资源指纹。
- **根因 / 约束**：服务器通过 `ssh zs` 管理，系统为 Alibaba Cloud Linux 4，Nginx 配置位于 `/etc/nginx/conf.d/jx3dps-online.conf`，站点根目录为 `/www/wwwroot/jx3dps-online/current`。默认仓库没有 Certbot 包；当前 Certbot 安装在 `/opt/certbot`，证书位于 `/etc/letsencrypt/live/www.wenshuijue.top`，续期由 `certbot-renew.timer` 执行。
- **正确做法**：生产构建上传到 `/www/wwwroot/jx3dps-online/releases/<时间戳>`，先核对关键文件 SHA-256，再备份并将 `current` 切换到新 release；Nginx 配置修改前必须备份并运行 `nginx -t`。HTTP 保留 `/.well-known/acme-challenge/`，其它请求跳转 HTTPS。跨 SSH 写多行脚本时避免本地 shell 提前展开远程变量，优先使用绝对路径。
- **验证方式**：确认 HTTP 返回 `301` 且 Location 为 HTTPS，HTTPS 返回 HTTP/2 `200` 和 HSTS；运行 `certbot renew --dry-run --no-random-sleep-on-renew`；对比公网首页、主 JS 与本地构建的 SHA-256，并确认 `readlink -f /www/wwwroot/jx3dps-online/current` 指向目标 release。
- **适用范围**：问水诀线上静态站点发布、Nginx 配置、Let’s Encrypt 证书签发与续期、线上版本核验和回滚。

## 新手引导：自动触发与手动入口分离

- **触发信号**：新用户关闭首次使用声明后自动出现 Tour，或老用户关闭新版本公告后因没有 `新手引导` 缓存而再次出现 Tour。
- **根因 / 约束**：自动触发位于 `src/功能模块/系统说明/当前版本/index.tsx` 的首次声明和版本公告关闭回调；右下角“新手教程”位于 `src/功能模块/系统工具/页面右下角工具/index.tsx`，属于用户主动入口，两者不能一起删除。
- **正确做法**：若产品要求取消首次自动指引，只移除“当前版本”组件对 `更新新手引导流程状态/更新当前引导步骤` 的 dispatch；保留 `新手引导` 组件、缓存映射和右下角手动教程入口。
- **验证方式**：运行 `npx jest --runTestsByPath src/功能模块/系统说明/当前版本/index.test.tsx --watchAll=false --runInBand`，确认关闭首次声明和版本公告都不会 dispatch 引导动作；同时用 `rg` 确认主动开启逻辑仍保留在右下角工具。
- **适用范围**：首次进入体验、新版本公告、新手 Tour、右下角系统工具和后续引导策略调整。

## 问水诀最优序列：随机分支伤害历史必须概率压缩

- **触发信号**：`600s` Beam Search 远超 `60s`，扩展节点未达到 `50,000`，堆内存接近或超过 `1 GiB`；短时长测试正常，但长轴候选复制成本随技能记录和伤害事件历史持续增长。
- **根因 / 约束**：每次随机分支若复制完整 `技能记录/伤害事件` 历史，并在候选评估时重新物化聚合，会让时间和内存随路径长度放大。压缩时不能只保存未加权次数，否则分支拆分、等价状态合并和最终期望伤害会失真；结束帧内尚未生效的事件也不能遗漏。
- **正确做法**：搜索分支使用绝对概率加权的 `压缩伤害`，每次动作后消费并清空已结算 `状态.伤害事件`；子分支按 `子概率 / 父概率` 缩放已有压缩项，等价分支按标准伤害键累加。compact/legacy 分支使用模式前缀隔离，最终聚合前推进到结束帧并再次压缩待生效事件。完整管线基准不得过滤零伤害技能，structured clone 后的伤害快照必须实际写入 Worker DTO。
- **验证方式**：运行两个显式基准：`RUN_WENSHUI_OPTIMIZER_BENCHMARK=1 npx jest --runTestsByPath src/功能模块/基础设置/公用设置/循环模拟/心法循环模拟/问水诀/search/beam-search.benchmark.test.ts --watchAll=false --runInBand` 和对应 `worker/full-pipeline.benchmark.test.ts`。两者必须完整扩展 `50,000 / 50,000`、以 `确定性预算` 结束、未提前结束且低于 `60s`；再运行问水诀优化器测试和生产构建。
- **适用范围**：问水诀最优序列搜索、随机会心分支、等价状态合并、策略聚合、Worker 性能基准，以及后续任何长时间轴概率搜索实现。

## 问水诀最优序列：贪心回退不能作为可信搜索下界

- **触发信号**：最优序列出现大量 `听雷-轻`，精确 DPS 反而低于当前内置循环；常见序列为多次听雷、切重剑、连续夕照。
- **根因 / 约束**：Excel `AY/AZ` 是技能时间轴模板，首个 token 可能与简化状态机起手不兼容。`构建问水合法基线` 失败后会生成贪心回退序列，该序列不能再作为 Beam incumbent；当前模拟器还未完整建模莺鸣柳、破招和四季剑法等派生链路。
- **正确做法**：搜索入口只允许 `来源 === 'Excel'` 的完整合法基线进入 `基线动作序列`，贪心回退仅用于诊断。最终结果必须按相同战斗时间精确复算当前循环，并以当前循环 DPS 作为安全下界；搜索未超过时展示“保留当前循环”并禁止应用劣化结果。
- **验证方式**：运行问水优化器目录测试，确认 `搜索结果不能低于同一战斗时间的当前循环基线` 通过；线上以 220 秒默认配置搜索时，结果不得低于当前循环，未超过时技能统计应显示当前循环而不是长串听雷。
- **适用范围**：问水诀最优序列 controller、Beam 基线、主线程精确复算、搜索结果展示与应用门禁。

## 问水诀最优序列：收敛结果不能丢弃会话 best

- **触发信号**：搜索过程中“当前最佳伤害”高于最终结果，或搜索空间收敛后正式重排只收到终点池候选；预设循环胜出时“技能序列”标签为空。
- **根因 / 约束**：Beam 的 `runtime.best` 可能来自合法基线或搜索中间的已知最高候选，不一定自然进入终点池。收敛时若用终点池首项覆盖 `best`，正式重排会遗漏已知最高候选。预设技能详情本身也不保证携带展示序列。
- **正确做法**：`获取问水Beam会话结果` 必须把 `runtime.best` 合并进有界候选池，再统一排序并令首项为 `最佳候选`。结果展示序列按“胜者序列 → 基线动作序列 → 已知最佳序列”依次兜底；三者都为空时明确失败，禁止返回空白序列。图标映射应放在无 React/CSS 依赖的纯模块中，供 UI 与完整管线共同校验。
- **验证方式**：运行 `beam-search.test.ts` 中“搜索收敛时不会丢弃高于终点池的已知最佳基线”，并运行 220 秒 `worker/full-pipeline.benchmark.test.ts`，确认候选列表包含 `最佳候选`、最终 DPS 不低于预设、序列非空且每个序列技能都有非兜底图标。
- **适用范围**：问水诀 Beam Search、Runner/Worker 候选协议、正式 DPS 重排、最优序列弹窗和技能图标展示。

## 共享循环入口：专属功能必须保留原心法模拟器

- **触发信号**：接入某个心法的专属按钮后，其他原本支持循环模拟的心法只剩禁用按钮，或生产构建通过但旧模拟器入口消失。
- **根因 / 约束**：`src/功能模块/基础设置/公用设置/循环模拟/index.tsx` 同时承载多心法懒加载模拟器和专属入口。用精简组件整体替换该文件会静默删除原 `CycleSimulatorContext`、支持心法映射和弹窗状态；类型检查无法发现这种行为回退。
- **正确做法**：保留原模拟器主体，通过薄的顶层组件按心法名称分流；问水诀渲染最优序列入口，其它心法继续渲染原通用循环模拟组件。不要在条件 return 后调用原组件 Hooks，应拆成两个独立子组件。
- **验证方式**：运行 `npx jest --runTestsByPath src/功能模块/基础设置/公用设置/循环模拟/index.test.tsx --watchAll=false --runInBand`，至少断言一个既有支持心法（当前用 `无方`）仍显示可点击的“循环模拟”按钮；再运行生产构建和问水诀浏览器冒烟。
- **适用范围**：循环模拟共享入口、心法专属工具按钮、懒加载模拟器、Context Provider，以及后续所有在共享组件中增加心法特例的改动。
